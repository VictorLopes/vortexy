import { App as vapp } from "./app";
import { Server as vserver, EmitterType as vemitterType } from "./server";

export namespace vortexy {
    export const App = vapp;
    export const Server = vserver;
    export const EmitterType = vemitterType;
};